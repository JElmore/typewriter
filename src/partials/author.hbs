{{! Everything inside the #primary_author tags pulls data from the author }}
{{#primary_author}}
<header class="box">

  {{#if profile_image}}
  <figure class="author-image">
    <div class="img" style="background-image: url({{img_url profile_image}})"><span class="hidden">{{name}}'s Picture</span></div>
  </figure>
  {{/if}}

  <div class="box__body">

    <h4 class="box__title">
      <a href="{{url}}">{{name}}</a>
    </h4>

    {{#if bio}}
    <p class="box__text">{{bio}}</p>
    {{/if}}

    <div class="box__text">

      {{#if location}}
      <span class="author-location">
        <icon name="ei-location" size="s" />
        {{location}}
      </span>
      {{/if}}

      {{#if website}}
      <span class="author-link">
        <icon name="ei-link" size="s" />
        <a href="{{website}}">{{website}}</a>
      </span>
      {{/if}}

      {{#if facebook}}
      <span class="author-link">
        <icon name="ei-sc-facebook" size="s" />
        <a href="{{facebook_url}}">{{name}}</a>
      </span>
      {{/if}}

      {{#if twitter}}
      <span class="author-link">
        <icon name="ei-sc-twitter" size="s" />
        <a href="{{twitter_url}}">{{twitter}}</a>
      </span>
      {{/if}}
    </div>

  </div>
</header>
{{/primary_author}}